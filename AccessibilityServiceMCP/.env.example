# Sao chép file này thành .env và cập nhật IP của thiết bị Android
# Copy this file to .env and update your Android device IP

# IP của thiết bị Android chạy Accessibility Service API
# IP of Android device running Accessibility Service API
ACCESSIBILITY_API_HOST=192.168.1.5

# Port của API (mặc định: 8080)
# API Port (default: 8080)  
ACCESSIBILITY_API_PORT=8080

# Hướng dẫn tìm IP thiết bị Android:
# How to find Android device IP:
# 1. Vào Settings > Wi-Fi > Chọn mạng đang kết nối
# 2. Hoặc dùng lệnh: adb shell ip route
# 3. Hoặc xem trong app Accessibility Service API
